Uses [react-sortable-tree](https://github.com/frontend-collective/react-sortable-tree) to recursively creates categories from a flat json

# Usage

*Development:*
```shell
$ npm start
```

*Clean dist folder:*
```shell
$ npm run clean
```

*Build:*
```shell
$ npm run build
```